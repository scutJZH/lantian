<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 设置为T_homework_detailsDAO接口方法提供sql语句配置 -->
<mapper namespace="org.scut.dao.T_Optional_ExerciseDAO">

    <insert id="insert">
        INSERT INTO optional_exercise(optional_exercise,optional_subject)
        VALUES
         <foreach collection="t_Selected_ExerciseVO" item="a" separator=",">
		(#{a.optional_exercise},#{a.optional_subject})
		</foreach>
		<!--collection：指定要遍历的集合 ; item：将当前遍历出的元素赋值给指定的变量  -->!>
    </insert>
	
</mapper>