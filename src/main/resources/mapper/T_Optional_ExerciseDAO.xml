<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 设置为T_homework_detailsDAO接口方法提供sql语句配置 -->
<mapper namespace="org.scut.dao.IT_Optional_ExerciseDAO">

    <select id="insert" parameterType="String" timeout="20">
       SELECT optional_exercise FROM t_optional_subject WHERE optional_subject = #{optional_subject}
    </select>

</mapper>