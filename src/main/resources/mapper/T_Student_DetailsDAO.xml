<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 设置为T_homework_detailsDAO接口方法提供sql语句配置 -->
<mapper namespace="org.scut.dao.T_Student_DetailsDAO">

    <select id="getClassList" resultType="T_Student_DetilsVO">
        SELECT * FROM t_student_details WHERE teacher_id = #{teacher_id}
    </select>
 <select id="queryStudent" resultType="T_Student_DetilsVO">
        SELECT * FROM t_student_details WHERE teacher_id = #{teacher_id} AND class_id=#{class_id} AND student_id=#{student_id}
    </select>
 
</mapper>