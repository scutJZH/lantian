<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 设置为IUserDao接口方法提供sql语句配置 -->
<mapper namespace="org.scut.dao.IT_Optional_SubjectDAO">

    <select id="get_Optional_Subject" resultType="T_Optional_SubjectVO" parameterType="String">
        SELECT * FROM t_optional_subject WHERE teacher_id = #{teacher_id}
    </select>
    
	
</mapper>