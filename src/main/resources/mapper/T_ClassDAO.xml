<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://www.mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 设置为T_homework_detailsDAO接口方法提供sql语句配置 -->
<mapper namespace="org.scut.dao.T_ClassDAO">

    <select id="get_Class" resultType="T_ClassVO" timeout="20">
        SELECT optional_class FROM t_class WHERE teacher_id = #{teacher_id}
    </select>
 	<insert id="addClass">
 		INSERT INTO t_class(teacher_id,class_id,grade,optional_class)
 		VALUES
 		(#{teacher_id},#{class_id},#{grade},#{class_number})
 	</insert>
</mapper>